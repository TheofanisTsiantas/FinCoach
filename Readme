
########################################################################
------------------------- PROJECT DEPENDENCIES -------------------------
########################################################################
Project dependencies: numpy, pandas, pyQt
To install use:
    python3 -m venv venv
    source venv/bin/activate
    python3 -m pip install numpy
    python3 -m pip install pandas
    python3 -m pip install PyQt5
    python3 -m pip install matplotlib

To check isntalled packages:
    python3 -m pip list

Ensure that venv interrpreter has been selected